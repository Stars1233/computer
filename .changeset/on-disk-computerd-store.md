---
"@cloudflare/dofs": patch
---

Support `computerd` persisting its workspace to disk instead via `COMPUTERD_DB=<path>` — see [the `computerd` README](../packages/computerd/README.md#on-disk-store).
